package com.maven.OnlineShoppingSB.entity;

public enum NotiMethod {
    IN_APP,   // Displayed inside the website/app
    EMAIL,    // Sent to the user's email
    SMS,      // Sent via text message
    PUSH      // Web or mobile push notification
}